name: nasa_picture_of_day_project
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  dartz:
  cupertino_icons: ^1.0.0
  get: ^3.24.0
  dio: ^3.0.10
  cached_network_image: ^2.5.0
  get_storage: ^1.4.0

dev_dependencies:
  mockito: ^4.1.3
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
